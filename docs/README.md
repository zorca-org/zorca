# Project assets

This directory contains the public assets used by the repository:

- `branding/`: ZOrca icon sources and regeneration instructions
- `images/`: images used by the repository README

ZOrca-specific product information lives in the root `README.md` and the
`website/` directory. Documentation for inherited editor features is maintained
by [Zed](https://zed.dev/docs).
