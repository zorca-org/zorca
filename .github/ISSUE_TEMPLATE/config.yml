# yaml-language-server: $schema=https://www.schemastore.org/github-issue-config.json
blank_issues_enabled: true
contact_links:
  - name: Upstream Zed issue
    url: https://github.com/zed-industries/zed/issues
    about: ZOrca is a fork of Zed. If the problem also reproduces in Zed, report it there instead — fixing it upstream fixes it here too.
